import { TranslatedProp } from "./CategorySwiper";

export interface Restaurant {
  id: number;
  username: string;
  backgroundColor: string;
  primaryColor: string;
  secondaryColor: string;
  backgroundURL: string;
  logoURL: string;
  name: TranslatedProp;
}
